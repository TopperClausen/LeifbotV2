const Description = "Reboots the bot, used only if the bot has encountered an issue";

let execute = (msg, q) => {
    q = [];
    process.exit(1);
}

module.exports = { execute: execute, Description: Description }